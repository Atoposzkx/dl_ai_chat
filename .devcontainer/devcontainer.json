{
  "name": "dl-ai-chat-tf1",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "workspaceFolder": "/workspace",
  "postCreateCommand": "python --version && python -c \"import tensorflow as tf; print(tf.__version__)\""
}
